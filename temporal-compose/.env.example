# Temporal Docker Compose Configuration
# Copy this file to .env and customize as needed

# Database Connection (PostgreSQL from Phase 2)
DB_HOST=postgres
DB_PORT=5432
DB_USER=temporal
DB_PASSWORD=temporal

# Temporal Server Ports
TEMPORAL_FRONTEND_PORT=7233    # gRPC endpoint for clients
TEMPORAL_METRICS_PORT=9090     # Prometheus metrics

# Temporal UI
TEMPORAL_UI_PORT=8080          # Web UI
TEMPORAL_CORS_ORIGINS=http://localhost:3000

# Notes:
# - DB_HOST should be 'postgres' (container name from postgres/docker-compose.yml)
# - Both temporal and postgres must be on the same 'temporal-network'
# - AUTO_SETUP=true in docker-compose.yml will create database schemas automatically

╔══════════════════════════════════════════════════════════════════════════╗
║                                                                          ║
║                    Temporal has been installed!                          ║
║                                                                          ║
╚══════════════════════════════════════════════════════════════════════════╝

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart Version: {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}

─────────────────────────────────────────────────────────────────────────────

📋 DEPLOYMENT STATUS

Check if pods are running:

  kubectl get pods -n {{ .Release.Namespace }}

Wait for all pods to be Ready:

  kubectl wait --for=condition=ready pod -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }} --timeout=5m

─────────────────────────────────────────────────────────────────────────────

🌐 ACCESS TEMPORAL UI

{{- if .Values.ui.enabled }}
{{- if .Values.ui.ingress.enabled }}

  Ingress is ENABLED. Access the UI at:

{{- range .Values.ui.ingress.hosts }}
  {{- if $.Values.ui.ingress.tls }}
  🔒 https://{{ .host }}
  {{- else }}
  🌍 http://{{ .host }}
  {{- end }}
{{- end }}

{{- else }}

  Ingress is DISABLED. Use port-forward to access the UI:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "temporal.fullname" . }}-ui 8080:{{ .Values.ui.service.port }}

  Then open: http://localhost:8080

{{- end }}
{{- else }}

  ⚠️  Temporal UI is DISABLED in values.yaml

{{- end }}

─────────────────────────────────────────────────────────────────────────────

🔌 CONNECT WORKERS

Temporal server is accessible at:

  From inside the cluster:
  {{ include "temporal.fullname" . }}-server.{{ .Release.Namespace }}.svc.cluster.local:7233

  From outside the cluster (port-forward):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "temporal.fullname" . }}-server 7233:7233

  Then connect to: localhost:7233

─────────────────────────────────────────────────────────────────────────────

📊 USEFUL COMMANDS

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} --tail=100

Describe resources:
  kubectl describe all -n {{ .Release.Namespace }}

Check service endpoints:
  kubectl get svc -n {{ .Release.Namespace }}

View configuration:
  kubectl get configmap -n {{ .Release.Namespace }}

─────────────────────────────────────────────────────────────────────────────

🗄️ DATABASE

{{- if .Values.database.external.enabled }}

  Using EXTERNAL database:
  Host: {{ .Values.database.external.host }}:{{ .Values.database.external.port }}
  Default DB: {{ .Values.database.external.defaultDatabase }}
  Visibility DB: {{ .Values.database.external.visibilityDatabase }}

  ⚠️  Ensure database schemas are set up before using Temporal!

{{- else }}

  ⚠️  No database configured! Temporal requires a database to function.

{{- end }}

─────────────────────────────────────────────────────────────────────────────

📚 NEXT STEPS

1. Create a namespace:

   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "temporal.fullname" . }}-server 7233:7233

   # In another terminal
   temporal operator namespace create default

2. Deploy your workers to the cluster

3. Start workflows via your application

4. Monitor workflows in the Temporal UI

─────────────────────────────────────────────────────────────────────────────

📖 DOCUMENTATION

- Temporal Docs: https://docs.temporal.io/
- HELM Chart README: See temporal-helm/README.md in your repository
- Local Testing Guide: See temporal-helm/LOCAL-TESTING.md

─────────────────────────────────────────────────────────────────────────────

Need help? Check the troubleshooting guide in the HELM chart README.

🎉 Happy orchestrating with Temporal!
